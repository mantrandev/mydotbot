interface:
  display_name: "iOS Test Failure Triage"
  short_description: "Explain failing tests before fixing"
  default_prompt: "Use $ios-test-failure-triage to explain this test failure, classify it, and tell me what to verify before making changes."
